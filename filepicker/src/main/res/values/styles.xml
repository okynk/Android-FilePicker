<resources>

    <!-- Base application theme. -->
    <style name="LibAppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:colorBackground">@android:color/background_light</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="LibAppTheme.Toolbar" parent="LibAppTheme">
        <item name="actionMenuTextColor">@color/action_menu_text_color</item>
        <item name="searchViewStyle">@style/LibAppTheme.Toolbar.SearchView</item>
        <item name="android:tint">@color/toolbar_home_button_color</item>
    </style>

    <style name="LibAppTheme.Toolbar.SearchView" parent="Widget.AppCompat.Light.SearchView">
        <item name="queryBackground">@null</item>
        <item name="queryHint">@string/searchview_query_hint</item>
    </style>

    <style name="PickerTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabBackground">@color/colorPrimary</item>
        <item name="tabGravity">fill</item>
        <item name="tabMaxWidth">0dp</item>
    </style>
    
    <style name="SmoothCheckBoxStyle">
        <item name="color_checked">@color/checkbox_color</item>
        <item name="color_unchecked">@android:color/white</item>
        <item name="color_unchecked_stroke">@color/checkbox_unchecked_color</item>
        <item name="color_tick">@android:color/white</item>
    </style>
</resources>
